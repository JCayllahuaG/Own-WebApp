import { NextSliderDirective } from './next-slider.directive';

describe('NextSliderDirective', () => {
  it('should create an instance', () => {
    const directive = new NextSliderDirective();
    expect(directive).toBeTruthy();
  });
});
