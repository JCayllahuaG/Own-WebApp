import { PrevSliderDirective } from './prev-slider.directive';

describe('PrevSliderDirective', () => {
  it('should create an instance', () => {
    const directive = new PrevSliderDirective();
    expect(directive).toBeTruthy();
  });
});
